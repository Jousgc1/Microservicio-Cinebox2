server:
  port: 8888
spring:
  cloud: 
    config:
      server: 
        git:
          default-label: config-server
          uri: https://github.com/Jousgc1/Microservicio-Cinebox2
          search-paths: config-data
  application:
    name: config-service
 
